# Diagramas — Disagro

Diagramas en [Mermaid](https://mermaid.js.org/): texto plano, versionable en git, renderizado
nativo en GitHub y en la mayoría de previews de Markdown (en VS Code con la extensión Markdown
Preview Mermaid Support).

- **[`diagramas-mermaid.md`](diagramas-mermaid.md)** — los 5 diagramas: arquitectura general,
  CQRS por módulo, modelo entidad-relación, secuencia de una confirmación (incluye el listado) y
  topología del deployment en Railway.

Reflejan el estado actual del proyecto (JWT de 3 min, deploy ya en producción en Railway, 15
ítems en el seed, listado de confirmaciones vía `GET /registrations`).
